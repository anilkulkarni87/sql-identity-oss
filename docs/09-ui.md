# Web UI

The web UI provides:
- Setup Wizard for initial configuration
- Dashboard for run metrics
- Explorer for cluster investigation

## Prerequisites

- Node.js 20+
- npm
- API reachable at `http://localhost:8000` (or proxied `/api`)

## Run Modes

Run API only (no standalone UI bundle):
```bash
idr serve
```
Open API docs at http://localhost:8000/docs.

Run full API + UI stack:
```bash
docker compose up --build
```
Open UI at http://localhost:3000.

Run UI in local dev mode:
```bash
cd idr_ui
npm ci --ignore-scripts --no-audit
npm run dev
```
Open UI at http://localhost:3000.

## Verification Commands

From `idr_ui/`:

```bash
npm run test
npm run test:e2e
```

Expected outcome:
- unit tests pass (`vitest`)
- Playwright smoke passes with:
  - dashboard shell render
  - setup wizard shell render
  - runs page shell render

## Setup Wizard
Steps:
1. Connect: choose platform and provide credentials
2. Discover: list tables and columns
3. Map: map identifiers and attributes
4. Rules: configure deterministic + fuzzy rules
5. Survivorship: configure golden record strategies
6. Review: save config and run

## Explorer
- Search by email/phone/ID
- View cluster graph and edges

## Dashboard
- Run summary and metrics
- Confidence scores
- Recent runs

## Troubleshooting

- API route and response expectations: `10-api.md`
- Common deployment/runtime issues: `14-troubleshooting.md`
- Golden-path checklists and CI gates: `33-golden-paths.md`
- UI auth and MCP operator runbook (`401`/`403` + deterministic checks): `38-ui-auth-mcp-operator-runbook.md`
